services:
  kibana:
    image: docker.elastic.co/kibana/kibana:8.14.3
    container_name: homelab-kibana
    restart: unless-stopped
    environment:
      ELASTICSEARCH_HOSTS: http://elasticsearch:9200
    ports:
      - 5601:5601
    depends_on:
      elasticsearch:
        condition: service_healthy
